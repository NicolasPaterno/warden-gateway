# Single-repo build context (this repo). Keep source + go.mod/go.sum; drop the rest.
.git
.github
bin/
gateway
*.log
coverage.out
.env

# Local Go workspace must never enter the image: it points at ../warden-auth,
# which does not exist inside the container, and would break `go build`.
go.work
go.work.sum
